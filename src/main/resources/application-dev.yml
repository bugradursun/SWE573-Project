spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres-sql-ctd
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgresSQLDialect

  mail:
    host: localhost
    port: 1025
    username: bugra
    password: bugra
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

application:
  security:
    jwt:
      secret-key: 8281102c287e4e901971370c9f4305c7
      expiration: 86400000
